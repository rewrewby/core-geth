name: Benchmark VM
# On demand only. The push trigger named `master`, which is no longer the default
# branch, so it was already dead; re-aiming it at `main` would spend up to six
# hours of runner time per push on a job that has never been run here. Start one
# deliberately when a benchmark is the question being asked.
on:
  workflow_dispatch:

# Least privilege. This workflow only reads the repository; the token needs
# nothing else, and the repository default is `write`.
permissions:
  contents: read

jobs:
  bench_core_geth:
    name: Benchmark core-geth
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    steps:
      - name: Set up Go 1.x
        id: go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version: '1.26'

      - name: Check out code into the Go module directory
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: 'recursive'

      - name: Benchmark ethereumclassic/core-geth
        id: bench
        run: |
          go test -short ./tests -count 1 -p 1 -timeout 60m -run NONE -bench=VM -v |& tee core-geth.txt

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: core-geth
          path: ./core-geth.txt

  bench_go_ethereum:
    name: Benchmark go-ethereum
    runs-on: ubuntu-24.04
    timeout-minutes: 360
    steps:
      - name: Set up Go 1.x
        id: go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version: '1.26'

      - name: Check out code into the Go module directory
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: 'recursive'

      - name: Benchmark ethereum/go-ethereum
        id: bench
        run: |
          git remote add foundation https://github.com/ethereum/go-ethereum.git
          git fetch foundation
          git checkout v1.11.5
          # git submodule update
          git checkout $GITHUB_SHA -- tests/vm_bench_test.go
          go test -short ./tests -count 1 -p 1 -timeout 60m -run NONE -bench=VM -v |& tee go-ethereum.txt

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: go-ethereum
          path: ./go-ethereum.txt

  compare:
    name: Compare
    needs: [bench_core_geth, bench_go_ethereum]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Set up Go 1.x
        id: go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7
        with:
          go-version: '1.26'

      - name: Check out code into the Go module directory
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Get dependencies
        run: |
          go install golang.org/x/perf/cmd/benchstat@v0.0.0-20260825160852-19be9d8e6c70

      - uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        name: Get go-ethereum artifact
        with:
          name: go-ethereum

      - uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        name: Get core-geth artifact
        with:
          name: core-geth

      - name: Prepare data
        run: |
          cat core-geth.txt | ./build/bench-suite-compress.sh | tee core-geth_compress.txt
          cat go-ethereum.txt | ./build/bench-suite-compress.sh | tee go-ethereum_compress.txt

      - name: "Analyze Results [COMPRESSED]"
        run: |
          benchstat go-ethereum_compress.txt core-geth_compress.txt

      - name: "Analyze Results [RAW DELTA]"
        run: |
          benchstat go-ethereum.txt core-geth.txt
