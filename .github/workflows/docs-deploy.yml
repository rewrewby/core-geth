name: Publish docs via GitHub Pages

# Controls when the action will run.
on:
  push:
    branches:
      - master
      - main
    paths:
      - mkdocs.yml
      - docs/**
      - requirements-mkdocs.txt

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# `mkdocs gh-deploy --force` pushes the built site to the gh-pages branch, so
# this is the one workflow here that genuinely needs write. Everything else in
# this repository declares `contents: read`.
permissions:
  contents: write

jobs:
  build:
    name: Deploy docs
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7
        with:
          python-version: '3.12'
      - run: pip install -r requirements-mkdocs.txt
      - run: mkdocs gh-deploy --force
